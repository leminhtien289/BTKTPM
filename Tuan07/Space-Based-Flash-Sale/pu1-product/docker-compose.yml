version: '3.8'

# Người 2 chạy file này
# Tạo file .env rồi điền IP máy chạy Redis vào REDIS_HOST

services:
  pu1-product:
    build: .
    ports:
      - "8081:8081"
    environment:
      - REDIS_HOST=${REDIS_HOST:-localhost}
    restart: unless-stopped
